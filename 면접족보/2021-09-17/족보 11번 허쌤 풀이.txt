int[] lotto = new int[6];
		
		//입력
		for(int i = 0; i<lotto.length;i++) {
			lotto[i] = (int) (Math.random()*10 + 1);
			
			//중복제거 로직
			//3 3 
			for(int j=0; j < i ; j++ ) {
				
				if(lotto[i] == lotto[j]) {
					i--;
					break;
				}
			}
			
		}
		
		//출력
		for(int i = 0; i<lotto.length;i++) {
			System.out.println(lotto[i]);
			 
		}
